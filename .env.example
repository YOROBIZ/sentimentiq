# SentimentIQ - Environment Configuration

# -- APP MODES --
# 'mock' (default) uses simulated Meta data and internal storage
# 'live' enables real FB/IG Graph API integration (requires App Review)
META_MODE=mock

# -- SERVER CONFIG --
PORT=3000
NODE_ENV=development

# -- META API (Only required for META_MODE=live) --
# META_APP_ID=your_app_id
# META_APP_SECRET=your_app_secret
# META_VERIFY_TOKEN=your_webhook_verify_token
